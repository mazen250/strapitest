const config = {
  translate:{
    en:{
      "app.title": "My App",
      "app.description": "My App Description",
    },
    }
  }
  


const bootstrap = (app) => {
  console.log(app);
};

export default {
  config,
  bootstrap,
};
